%h1= show_diff(h(@version.title), h(@document.title))

:textile
  #{show_diff(h(@version.body), h(@document.body))}

= partial 'common/versions', :model => @document

= link_to 'Back', resource(@group, @document)
