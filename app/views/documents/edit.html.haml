%h1
  Documents controller, edit action

= error_messages_for @document

= form_for(@document, :action => resource(@group, @document)) do
  %p
    = text_field :title, :label => 'Title'
  %p
    = text_area :body, :label => 'Body'
  %p
    = submit "Update"

= link_to 'Show', resource(@group, @document)
|
= link_to 'Back', resource(@group, :documents)
