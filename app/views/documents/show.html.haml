%h1&= @version.title
%p
  %i== edited by #{h User.get(@version.user_id).login} #{show_time_ago @version.updated_at}

%p
  :textile
    #{h @version.body}

= partial 'common/versions', :model => @document

= link_to 'Edit',  resource(@group, @document, :edit)
|
= link_to 'Back', resource(@group, :documents)
