%h1== Artifacts in #{h @category.name}

-# TODO replace group with @category.group when DM works
- group = Group.get @category.group.id

%table
  %tr
  - for artifact in @artifacts
    %tr
      %td
        = link_to 'Show', resource(group, @category, artifact)
      %td
        = link_to 'Edit', resource(group, @category, artifact, :edit)

%p
  = link_to 'New', resource(group, @category, :artifacts, :new)
  %br
  = link_to "Back to #{h @category.name}", resource(group, @category)
