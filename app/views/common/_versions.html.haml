- unless model.versions.empty?
  %p
    Version
    - model.versions.size.times do |i|
      - j = i + 1
      - if params[:version] and params[:version].to_i.eql? j
        = j
      - else
        = link_to j, resource(@group, model, :version => j)
      |
    - if params[:version]
      = link_to 'current', resource(@group, model)
    - else
      current
    = "| #{link_to 'diff', resource(@group, model, :diff, :version => params[:version])}" if params[:version]
