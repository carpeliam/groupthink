%h1
  Groups
%table
  %tr
    %th Name
  - for group in @groups
    %tr
      %td
        = link_to h(group.name), resource(group)
      %td
        = link_to 'Edit', resource(group, :edit)

= link_to 'New', resource(:groups, :new)

= will_paginate @groups
